# dotfiles

## Installation

```bash
# First, move into `install` directory, then add permission to the setup script.
cd install
chmod 755 init.sh

# Execute the script; it should create symbolic links in your machine.
./init.sh
```
